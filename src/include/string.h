int len(char * mychar);
int startswith(char * mainchar, char * start);
/*
class string
{
public:
    char * words;
    int length();
    int startswith(char * start);
    int endswith(char * end);
    string(char * w);
}
*/
